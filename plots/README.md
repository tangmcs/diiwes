# Generated Plots

This directory contains generated figures and tables. New products are ignored
by Git; only this policy note is part of the current publishable tree.

The figures previously tracked here predate the mentor-requested
no-trust-region study. They were moved to the ignored local archive at
`analysis/legacy_trust_confounded_plots/` and remain recoverable from Git
history. They mix trust-radius and other experimental choices that the current
controlled comparisons exclude, so they must not be cited as current evidence.

Use the self-contained
[`../reports/hopper_hessian_no_trust/mentor_report.html`](../reports/hopper_hessian_no_trust/mentor_report.html)
for the basic Standard ES versus curvature result. The corresponding protocol,
validation record, and claim boundaries are indexed in
[`../docs/README.md`](../docs/README.md).
